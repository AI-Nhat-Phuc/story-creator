# OpenAI API Configuration
# Lấy API key từ: https://platform.openai.com/api-keys

OPENAI_API_KEY=sk-your-api-key-here

# Thay "sk-your-api-key-here" bằng API key thật của bạn

# ============================================
# OAuth Configuration (Frontend)
# ============================================

# Google OAuth
# Lấy từ: https://console.cloud.google.com/apis/credentials
# 1. Tạo project mới hoặc chọn project có sẵn
# 2. Vào "APIs & Services" > "Credentials"
# 3. Click "Create Credentials" > "OAuth 2.0 Client ID"
# 4. Application type: "Web application"
# 5. Authorized JavaScript origins: http://localhost:3000, http://localhost:5173
# 6. Authorized redirect URIs: http://localhost:3000, http://localhost:5173
# 7. Copy "Client ID" vào đây
# LƯU Ý: Không cần Client Secret cho frontend OAuth (token verification flow)
VITE_GOOGLE_CLIENT_ID=your_google_client_id_here

# Facebook OAuth
# Lấy từ: https://developers.facebook.com/apps
# 1. Tạo app mới hoặc chọn app có sẵn
# 2. Vào "Settings" > "Basic"
# 3. Copy "App ID" vào đây
# 4. Thêm "http://localhost:3000" vào "App Domains"
# 5. Bật "Facebook Login" trong Products
VITE_FACEBOOK_APP_ID=your_facebook_app_id_here

# ============================================
# JWT Authentication
# ============================================

# Secret key for signing JWT tokens
# Tạo key ngẫu nhiên: python -c "import secrets; print(secrets.token_hex(32))"
# QUAN TRỌNG: Phải set trên Vercel để token không bị reset sau cold start
JWT_SECRET_KEY=your-random-secret-key-here

# ============================================
# MongoDB Atlas (Production - Vercel)
# ============================================

# Connection string cho MongoDB Atlas (persistent cloud database)
# Lấy từ: https://cloud.mongodb.com
# 1. Tạo Free Cluster (M0, 512MB miễn phí)
# 2. Database Access → tạo user (username/password)
# 3. Network Access → Add IP: 0.0.0.0/0 (cho phép Vercel truy cập)
# 4. Database → Connect → Drivers → Python → Copy connection string
# 5. Thay <password> bằng password đã tạo ở bước 2
# Nếu không set → app sẽ dùng TinyDB (local dev, data mất khi Vercel cold start)
MONGODB_URI=mongodb+srv://user:password@cluster0.xxxxx.mongodb.net/story_creator?retryWrites=true&w=majority
